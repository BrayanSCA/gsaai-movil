import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gsaai_moviles',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
